query GetTestSession($id: ID!) {
  testSession(id: $id) {
    ...TestSessionFragment
    orderedQuestions {
      questionId
      order
    }
    test {
      ...TestFragment
    }
    questions {
      id
      questionText
      correctOptionCount
      options {
        id
        optionText
      }
    }
  }
} 