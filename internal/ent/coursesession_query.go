// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"
	"template/internal/ent/coursesection"
	"template/internal/ent/coursesession"
	"template/internal/ent/predicate"
	"template/internal/ent/user"
	"template/internal/ent/userquestionanswer"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// CourseSessionQuery is the builder for querying CourseSession entities.
type CourseSessionQuery struct {
	config
	ctx                     *QueryContext
	order                   []coursesession.OrderOption
	inters                  []Interceptor
	predicates              []predicate.CourseSession
	withUser                *UserQuery
	withCourseSection       *CourseSectionQuery
	withUserQuestionAnswers *UserQuestionAnswerQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CourseSessionQuery builder.
func (csq *CourseSessionQuery) Where(ps ...predicate.CourseSession) *CourseSessionQuery {
	csq.predicates = append(csq.predicates, ps...)
	return csq
}

// Limit the number of records to be returned by this query.
func (csq *CourseSessionQuery) Limit(limit int) *CourseSessionQuery {
	csq.ctx.Limit = &limit
	return csq
}

// Offset to start from.
func (csq *CourseSessionQuery) Offset(offset int) *CourseSessionQuery {
	csq.ctx.Offset = &offset
	return csq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (csq *CourseSessionQuery) Unique(unique bool) *CourseSessionQuery {
	csq.ctx.Unique = &unique
	return csq
}

// Order specifies how the records should be ordered.
func (csq *CourseSessionQuery) Order(o ...coursesession.OrderOption) *CourseSessionQuery {
	csq.order = append(csq.order, o...)
	return csq
}

// QueryUser chains the current query on the "user" edge.
func (csq *CourseSessionQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: csq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(coursesession.Table, coursesession.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, coursesession.UserTable, coursesession.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(csq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryCourseSection chains the current query on the "course_section" edge.
func (csq *CourseSessionQuery) QueryCourseSection() *CourseSectionQuery {
	query := (&CourseSectionClient{config: csq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(coursesession.Table, coursesession.FieldID, selector),
			sqlgraph.To(coursesection.Table, coursesection.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, coursesession.CourseSectionTable, coursesession.CourseSectionColumn),
		)
		fromU = sqlgraph.SetNeighbors(csq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUserQuestionAnswers chains the current query on the "user_question_answers" edge.
func (csq *CourseSessionQuery) QueryUserQuestionAnswers() *UserQuestionAnswerQuery {
	query := (&UserQuestionAnswerClient{config: csq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := csq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := csq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(coursesession.Table, coursesession.FieldID, selector),
			sqlgraph.To(userquestionanswer.Table, userquestionanswer.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, coursesession.UserQuestionAnswersTable, coursesession.UserQuestionAnswersColumn),
		)
		fromU = sqlgraph.SetNeighbors(csq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first CourseSession entity from the query.
// Returns a *NotFoundError when no CourseSession was found.
func (csq *CourseSessionQuery) First(ctx context.Context) (*CourseSession, error) {
	nodes, err := csq.Limit(1).All(setContextOp(ctx, csq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{coursesession.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (csq *CourseSessionQuery) FirstX(ctx context.Context) *CourseSession {
	node, err := csq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first CourseSession ID from the query.
// Returns a *NotFoundError when no CourseSession ID was found.
func (csq *CourseSessionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csq.Limit(1).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{coursesession.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (csq *CourseSessionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := csq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single CourseSession entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one CourseSession entity is found.
// Returns a *NotFoundError when no CourseSession entities are found.
func (csq *CourseSessionQuery) Only(ctx context.Context) (*CourseSession, error) {
	nodes, err := csq.Limit(2).All(setContextOp(ctx, csq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{coursesession.Label}
	default:
		return nil, &NotSingularError{coursesession.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (csq *CourseSessionQuery) OnlyX(ctx context.Context) *CourseSession {
	node, err := csq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only CourseSession ID in the query.
// Returns a *NotSingularError when more than one CourseSession ID is found.
// Returns a *NotFoundError when no entities are found.
func (csq *CourseSessionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = csq.Limit(2).IDs(setContextOp(ctx, csq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{coursesession.Label}
	default:
		err = &NotSingularError{coursesession.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (csq *CourseSessionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := csq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of CourseSessions.
func (csq *CourseSessionQuery) All(ctx context.Context) ([]*CourseSession, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryAll)
	if err := csq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*CourseSession, *CourseSessionQuery]()
	return withInterceptors[[]*CourseSession](ctx, csq, qr, csq.inters)
}

// AllX is like All, but panics if an error occurs.
func (csq *CourseSessionQuery) AllX(ctx context.Context) []*CourseSession {
	nodes, err := csq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of CourseSession IDs.
func (csq *CourseSessionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if csq.ctx.Unique == nil && csq.path != nil {
		csq.Unique(true)
	}
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryIDs)
	if err = csq.Select(coursesession.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (csq *CourseSessionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := csq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (csq *CourseSessionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryCount)
	if err := csq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, csq, querierCount[*CourseSessionQuery](), csq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (csq *CourseSessionQuery) CountX(ctx context.Context) int {
	count, err := csq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (csq *CourseSessionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, csq.ctx, ent.OpQueryExist)
	switch _, err := csq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (csq *CourseSessionQuery) ExistX(ctx context.Context) bool {
	exist, err := csq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CourseSessionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (csq *CourseSessionQuery) Clone() *CourseSessionQuery {
	if csq == nil {
		return nil
	}
	return &CourseSessionQuery{
		config:                  csq.config,
		ctx:                     csq.ctx.Clone(),
		order:                   append([]coursesession.OrderOption{}, csq.order...),
		inters:                  append([]Interceptor{}, csq.inters...),
		predicates:              append([]predicate.CourseSession{}, csq.predicates...),
		withUser:                csq.withUser.Clone(),
		withCourseSection:       csq.withCourseSection.Clone(),
		withUserQuestionAnswers: csq.withUserQuestionAnswers.Clone(),
		// clone intermediate query.
		sql:  csq.sql.Clone(),
		path: csq.path,
	}
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (csq *CourseSessionQuery) WithUser(opts ...func(*UserQuery)) *CourseSessionQuery {
	query := (&UserClient{config: csq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csq.withUser = query
	return csq
}

// WithCourseSection tells the query-builder to eager-load the nodes that are connected to
// the "course_section" edge. The optional arguments are used to configure the query builder of the edge.
func (csq *CourseSessionQuery) WithCourseSection(opts ...func(*CourseSectionQuery)) *CourseSessionQuery {
	query := (&CourseSectionClient{config: csq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csq.withCourseSection = query
	return csq
}

// WithUserQuestionAnswers tells the query-builder to eager-load the nodes that are connected to
// the "user_question_answers" edge. The optional arguments are used to configure the query builder of the edge.
func (csq *CourseSessionQuery) WithUserQuestionAnswers(opts ...func(*UserQuestionAnswerQuery)) *CourseSessionQuery {
	query := (&UserQuestionAnswerClient{config: csq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	csq.withUserQuestionAnswers = query
	return csq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.CourseSession.Query().
//		GroupBy(coursesession.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (csq *CourseSessionQuery) GroupBy(field string, fields ...string) *CourseSessionGroupBy {
	csq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CourseSessionGroupBy{build: csq}
	grbuild.flds = &csq.ctx.Fields
	grbuild.label = coursesession.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.CourseSession.Query().
//		Select(coursesession.FieldCreatedAt).
//		Scan(ctx, &v)
func (csq *CourseSessionQuery) Select(fields ...string) *CourseSessionSelect {
	csq.ctx.Fields = append(csq.ctx.Fields, fields...)
	sbuild := &CourseSessionSelect{CourseSessionQuery: csq}
	sbuild.label = coursesession.Label
	sbuild.flds, sbuild.scan = &csq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CourseSessionSelect configured with the given aggregations.
func (csq *CourseSessionQuery) Aggregate(fns ...AggregateFunc) *CourseSessionSelect {
	return csq.Select().Aggregate(fns...)
}

func (csq *CourseSessionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range csq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, csq); err != nil {
				return err
			}
		}
	}
	for _, f := range csq.ctx.Fields {
		if !coursesession.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if csq.path != nil {
		prev, err := csq.path(ctx)
		if err != nil {
			return err
		}
		csq.sql = prev
	}
	return nil
}

func (csq *CourseSessionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*CourseSession, error) {
	var (
		nodes       = []*CourseSession{}
		_spec       = csq.querySpec()
		loadedTypes = [3]bool{
			csq.withUser != nil,
			csq.withCourseSection != nil,
			csq.withUserQuestionAnswers != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*CourseSession).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &CourseSession{config: csq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, csq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := csq.withUser; query != nil {
		if err := csq.loadUser(ctx, query, nodes, nil,
			func(n *CourseSession, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := csq.withCourseSection; query != nil {
		if err := csq.loadCourseSection(ctx, query, nodes, nil,
			func(n *CourseSession, e *CourseSection) { n.Edges.CourseSection = e }); err != nil {
			return nil, err
		}
	}
	if query := csq.withUserQuestionAnswers; query != nil {
		if err := csq.loadUserQuestionAnswers(ctx, query, nodes,
			func(n *CourseSession) { n.Edges.UserQuestionAnswers = []*UserQuestionAnswer{} },
			func(n *CourseSession, e *UserQuestionAnswer) {
				n.Edges.UserQuestionAnswers = append(n.Edges.UserQuestionAnswers, e)
			}); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (csq *CourseSessionQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*CourseSession, init func(*CourseSession), assign func(*CourseSession, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CourseSession)
	for i := range nodes {
		fk := nodes[i].UserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csq *CourseSessionQuery) loadCourseSection(ctx context.Context, query *CourseSectionQuery, nodes []*CourseSession, init func(*CourseSession), assign func(*CourseSession, *CourseSection)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*CourseSession)
	for i := range nodes {
		if nodes[i].CourseSectionID == nil {
			continue
		}
		fk := *nodes[i].CourseSectionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(coursesection.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "course_section_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (csq *CourseSessionQuery) loadUserQuestionAnswers(ctx context.Context, query *UserQuestionAnswerQuery, nodes []*CourseSession, init func(*CourseSession), assign func(*CourseSession, *UserQuestionAnswer)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*CourseSession)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	if len(query.ctx.Fields) > 0 {
		query.ctx.AppendFieldOnce(userquestionanswer.FieldSessionID)
	}
	query.Where(predicate.UserQuestionAnswer(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(coursesession.UserQuestionAnswersColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.SessionID
		node, ok := nodeids[fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "session_id" returned %v for node %v`, fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (csq *CourseSessionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := csq.querySpec()
	_spec.Node.Columns = csq.ctx.Fields
	if len(csq.ctx.Fields) > 0 {
		_spec.Unique = csq.ctx.Unique != nil && *csq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, csq.driver, _spec)
}

func (csq *CourseSessionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(coursesession.Table, coursesession.Columns, sqlgraph.NewFieldSpec(coursesession.FieldID, field.TypeUUID))
	_spec.From = csq.sql
	if unique := csq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if csq.path != nil {
		_spec.Unique = true
	}
	if fields := csq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, coursesession.FieldID)
		for i := range fields {
			if fields[i] != coursesession.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if csq.withUser != nil {
			_spec.Node.AddColumnOnce(coursesession.FieldUserID)
		}
		if csq.withCourseSection != nil {
			_spec.Node.AddColumnOnce(coursesession.FieldCourseSectionID)
		}
	}
	if ps := csq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := csq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := csq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := csq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (csq *CourseSessionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(csq.driver.Dialect())
	t1 := builder.Table(coursesession.Table)
	columns := csq.ctx.Fields
	if len(columns) == 0 {
		columns = coursesession.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if csq.sql != nil {
		selector = csq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if csq.ctx.Unique != nil && *csq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range csq.predicates {
		p(selector)
	}
	for _, p := range csq.order {
		p(selector)
	}
	if offset := csq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := csq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// CourseSessionGroupBy is the group-by builder for CourseSession entities.
type CourseSessionGroupBy struct {
	selector
	build *CourseSessionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (csgb *CourseSessionGroupBy) Aggregate(fns ...AggregateFunc) *CourseSessionGroupBy {
	csgb.fns = append(csgb.fns, fns...)
	return csgb
}

// Scan applies the selector query and scans the result into the given value.
func (csgb *CourseSessionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, csgb.build.ctx, ent.OpQueryGroupBy)
	if err := csgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CourseSessionQuery, *CourseSessionGroupBy](ctx, csgb.build, csgb, csgb.build.inters, v)
}

func (csgb *CourseSessionGroupBy) sqlScan(ctx context.Context, root *CourseSessionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(csgb.fns))
	for _, fn := range csgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*csgb.flds)+len(csgb.fns))
		for _, f := range *csgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*csgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := csgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CourseSessionSelect is the builder for selecting fields of CourseSession entities.
type CourseSessionSelect struct {
	*CourseSessionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (css *CourseSessionSelect) Aggregate(fns ...AggregateFunc) *CourseSessionSelect {
	css.fns = append(css.fns, fns...)
	return css
}

// Scan applies the selector query and scans the result into the given value.
func (css *CourseSessionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, css.ctx, ent.OpQuerySelect)
	if err := css.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CourseSessionQuery, *CourseSessionSelect](ctx, css.CourseSessionQuery, css, css.inters, v)
}

func (css *CourseSessionSelect) sqlScan(ctx context.Context, root *CourseSessionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(css.fns))
	for _, fn := range css.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*css.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := css.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
