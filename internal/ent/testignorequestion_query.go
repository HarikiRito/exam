// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"template/internal/ent/predicate"
	"template/internal/ent/question"
	"template/internal/ent/test"
	"template/internal/ent/testignorequestion"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// TestIgnoreQuestionQuery is the builder for querying TestIgnoreQuestion entities.
type TestIgnoreQuestionQuery struct {
	config
	ctx          *QueryContext
	order        []testignorequestion.OrderOption
	inters       []Interceptor
	predicates   []predicate.TestIgnoreQuestion
	withTest     *TestQuery
	withQuestion *QuestionQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TestIgnoreQuestionQuery builder.
func (tiqq *TestIgnoreQuestionQuery) Where(ps ...predicate.TestIgnoreQuestion) *TestIgnoreQuestionQuery {
	tiqq.predicates = append(tiqq.predicates, ps...)
	return tiqq
}

// Limit the number of records to be returned by this query.
func (tiqq *TestIgnoreQuestionQuery) Limit(limit int) *TestIgnoreQuestionQuery {
	tiqq.ctx.Limit = &limit
	return tiqq
}

// Offset to start from.
func (tiqq *TestIgnoreQuestionQuery) Offset(offset int) *TestIgnoreQuestionQuery {
	tiqq.ctx.Offset = &offset
	return tiqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tiqq *TestIgnoreQuestionQuery) Unique(unique bool) *TestIgnoreQuestionQuery {
	tiqq.ctx.Unique = &unique
	return tiqq
}

// Order specifies how the records should be ordered.
func (tiqq *TestIgnoreQuestionQuery) Order(o ...testignorequestion.OrderOption) *TestIgnoreQuestionQuery {
	tiqq.order = append(tiqq.order, o...)
	return tiqq
}

// QueryTest chains the current query on the "test" edge.
func (tiqq *TestIgnoreQuestionQuery) QueryTest() *TestQuery {
	query := (&TestClient{config: tiqq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tiqq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tiqq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(testignorequestion.Table, testignorequestion.FieldID, selector),
			sqlgraph.To(test.Table, test.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, testignorequestion.TestTable, testignorequestion.TestColumn),
		)
		fromU = sqlgraph.SetNeighbors(tiqq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryQuestion chains the current query on the "question" edge.
func (tiqq *TestIgnoreQuestionQuery) QueryQuestion() *QuestionQuery {
	query := (&QuestionClient{config: tiqq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tiqq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tiqq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(testignorequestion.Table, testignorequestion.FieldID, selector),
			sqlgraph.To(question.Table, question.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, testignorequestion.QuestionTable, testignorequestion.QuestionColumn),
		)
		fromU = sqlgraph.SetNeighbors(tiqq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TestIgnoreQuestion entity from the query.
// Returns a *NotFoundError when no TestIgnoreQuestion was found.
func (tiqq *TestIgnoreQuestionQuery) First(ctx context.Context) (*TestIgnoreQuestion, error) {
	nodes, err := tiqq.Limit(1).All(setContextOp(ctx, tiqq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{testignorequestion.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) FirstX(ctx context.Context) *TestIgnoreQuestion {
	node, err := tiqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TestIgnoreQuestion ID from the query.
// Returns a *NotFoundError when no TestIgnoreQuestion ID was found.
func (tiqq *TestIgnoreQuestionQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tiqq.Limit(1).IDs(setContextOp(ctx, tiqq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{testignorequestion.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tiqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TestIgnoreQuestion entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TestIgnoreQuestion entity is found.
// Returns a *NotFoundError when no TestIgnoreQuestion entities are found.
func (tiqq *TestIgnoreQuestionQuery) Only(ctx context.Context) (*TestIgnoreQuestion, error) {
	nodes, err := tiqq.Limit(2).All(setContextOp(ctx, tiqq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{testignorequestion.Label}
	default:
		return nil, &NotSingularError{testignorequestion.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) OnlyX(ctx context.Context) *TestIgnoreQuestion {
	node, err := tiqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TestIgnoreQuestion ID in the query.
// Returns a *NotSingularError when more than one TestIgnoreQuestion ID is found.
// Returns a *NotFoundError when no entities are found.
func (tiqq *TestIgnoreQuestionQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tiqq.Limit(2).IDs(setContextOp(ctx, tiqq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{testignorequestion.Label}
	default:
		err = &NotSingularError{testignorequestion.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tiqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TestIgnoreQuestions.
func (tiqq *TestIgnoreQuestionQuery) All(ctx context.Context) ([]*TestIgnoreQuestion, error) {
	ctx = setContextOp(ctx, tiqq.ctx, ent.OpQueryAll)
	if err := tiqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TestIgnoreQuestion, *TestIgnoreQuestionQuery]()
	return withInterceptors[[]*TestIgnoreQuestion](ctx, tiqq, qr, tiqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) AllX(ctx context.Context) []*TestIgnoreQuestion {
	nodes, err := tiqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TestIgnoreQuestion IDs.
func (tiqq *TestIgnoreQuestionQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if tiqq.ctx.Unique == nil && tiqq.path != nil {
		tiqq.Unique(true)
	}
	ctx = setContextOp(ctx, tiqq.ctx, ent.OpQueryIDs)
	if err = tiqq.Select(testignorequestion.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tiqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tiqq *TestIgnoreQuestionQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tiqq.ctx, ent.OpQueryCount)
	if err := tiqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tiqq, querierCount[*TestIgnoreQuestionQuery](), tiqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) CountX(ctx context.Context) int {
	count, err := tiqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tiqq *TestIgnoreQuestionQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tiqq.ctx, ent.OpQueryExist)
	switch _, err := tiqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tiqq *TestIgnoreQuestionQuery) ExistX(ctx context.Context) bool {
	exist, err := tiqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TestIgnoreQuestionQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tiqq *TestIgnoreQuestionQuery) Clone() *TestIgnoreQuestionQuery {
	if tiqq == nil {
		return nil
	}
	return &TestIgnoreQuestionQuery{
		config:       tiqq.config,
		ctx:          tiqq.ctx.Clone(),
		order:        append([]testignorequestion.OrderOption{}, tiqq.order...),
		inters:       append([]Interceptor{}, tiqq.inters...),
		predicates:   append([]predicate.TestIgnoreQuestion{}, tiqq.predicates...),
		withTest:     tiqq.withTest.Clone(),
		withQuestion: tiqq.withQuestion.Clone(),
		// clone intermediate query.
		sql:  tiqq.sql.Clone(),
		path: tiqq.path,
	}
}

// WithTest tells the query-builder to eager-load the nodes that are connected to
// the "test" edge. The optional arguments are used to configure the query builder of the edge.
func (tiqq *TestIgnoreQuestionQuery) WithTest(opts ...func(*TestQuery)) *TestIgnoreQuestionQuery {
	query := (&TestClient{config: tiqq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tiqq.withTest = query
	return tiqq
}

// WithQuestion tells the query-builder to eager-load the nodes that are connected to
// the "question" edge. The optional arguments are used to configure the query builder of the edge.
func (tiqq *TestIgnoreQuestionQuery) WithQuestion(opts ...func(*QuestionQuery)) *TestIgnoreQuestionQuery {
	query := (&QuestionClient{config: tiqq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tiqq.withQuestion = query
	return tiqq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TestIgnoreQuestion.Query().
//		GroupBy(testignorequestion.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tiqq *TestIgnoreQuestionQuery) GroupBy(field string, fields ...string) *TestIgnoreQuestionGroupBy {
	tiqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TestIgnoreQuestionGroupBy{build: tiqq}
	grbuild.flds = &tiqq.ctx.Fields
	grbuild.label = testignorequestion.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TestIgnoreQuestion.Query().
//		Select(testignorequestion.FieldCreatedAt).
//		Scan(ctx, &v)
func (tiqq *TestIgnoreQuestionQuery) Select(fields ...string) *TestIgnoreQuestionSelect {
	tiqq.ctx.Fields = append(tiqq.ctx.Fields, fields...)
	sbuild := &TestIgnoreQuestionSelect{TestIgnoreQuestionQuery: tiqq}
	sbuild.label = testignorequestion.Label
	sbuild.flds, sbuild.scan = &tiqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TestIgnoreQuestionSelect configured with the given aggregations.
func (tiqq *TestIgnoreQuestionQuery) Aggregate(fns ...AggregateFunc) *TestIgnoreQuestionSelect {
	return tiqq.Select().Aggregate(fns...)
}

func (tiqq *TestIgnoreQuestionQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tiqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tiqq); err != nil {
				return err
			}
		}
	}
	for _, f := range tiqq.ctx.Fields {
		if !testignorequestion.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tiqq.path != nil {
		prev, err := tiqq.path(ctx)
		if err != nil {
			return err
		}
		tiqq.sql = prev
	}
	return nil
}

func (tiqq *TestIgnoreQuestionQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TestIgnoreQuestion, error) {
	var (
		nodes       = []*TestIgnoreQuestion{}
		_spec       = tiqq.querySpec()
		loadedTypes = [2]bool{
			tiqq.withTest != nil,
			tiqq.withQuestion != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TestIgnoreQuestion).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TestIgnoreQuestion{config: tiqq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tiqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tiqq.withTest; query != nil {
		if err := tiqq.loadTest(ctx, query, nodes, nil,
			func(n *TestIgnoreQuestion, e *Test) { n.Edges.Test = e }); err != nil {
			return nil, err
		}
	}
	if query := tiqq.withQuestion; query != nil {
		if err := tiqq.loadQuestion(ctx, query, nodes, nil,
			func(n *TestIgnoreQuestion, e *Question) { n.Edges.Question = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tiqq *TestIgnoreQuestionQuery) loadTest(ctx context.Context, query *TestQuery, nodes []*TestIgnoreQuestion, init func(*TestIgnoreQuestion), assign func(*TestIgnoreQuestion, *Test)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*TestIgnoreQuestion)
	for i := range nodes {
		fk := nodes[i].TestID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(test.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "test_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (tiqq *TestIgnoreQuestionQuery) loadQuestion(ctx context.Context, query *QuestionQuery, nodes []*TestIgnoreQuestion, init func(*TestIgnoreQuestion), assign func(*TestIgnoreQuestion, *Question)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*TestIgnoreQuestion)
	for i := range nodes {
		fk := nodes[i].QuestionID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(question.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "question_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tiqq *TestIgnoreQuestionQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tiqq.querySpec()
	_spec.Node.Columns = tiqq.ctx.Fields
	if len(tiqq.ctx.Fields) > 0 {
		_spec.Unique = tiqq.ctx.Unique != nil && *tiqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tiqq.driver, _spec)
}

func (tiqq *TestIgnoreQuestionQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(testignorequestion.Table, testignorequestion.Columns, sqlgraph.NewFieldSpec(testignorequestion.FieldID, field.TypeUUID))
	_spec.From = tiqq.sql
	if unique := tiqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tiqq.path != nil {
		_spec.Unique = true
	}
	if fields := tiqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, testignorequestion.FieldID)
		for i := range fields {
			if fields[i] != testignorequestion.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if tiqq.withTest != nil {
			_spec.Node.AddColumnOnce(testignorequestion.FieldTestID)
		}
		if tiqq.withQuestion != nil {
			_spec.Node.AddColumnOnce(testignorequestion.FieldQuestionID)
		}
	}
	if ps := tiqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tiqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tiqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tiqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tiqq *TestIgnoreQuestionQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tiqq.driver.Dialect())
	t1 := builder.Table(testignorequestion.Table)
	columns := tiqq.ctx.Fields
	if len(columns) == 0 {
		columns = testignorequestion.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tiqq.sql != nil {
		selector = tiqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tiqq.ctx.Unique != nil && *tiqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tiqq.predicates {
		p(selector)
	}
	for _, p := range tiqq.order {
		p(selector)
	}
	if offset := tiqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tiqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TestIgnoreQuestionGroupBy is the group-by builder for TestIgnoreQuestion entities.
type TestIgnoreQuestionGroupBy struct {
	selector
	build *TestIgnoreQuestionQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tiqgb *TestIgnoreQuestionGroupBy) Aggregate(fns ...AggregateFunc) *TestIgnoreQuestionGroupBy {
	tiqgb.fns = append(tiqgb.fns, fns...)
	return tiqgb
}

// Scan applies the selector query and scans the result into the given value.
func (tiqgb *TestIgnoreQuestionGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tiqgb.build.ctx, ent.OpQueryGroupBy)
	if err := tiqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestIgnoreQuestionQuery, *TestIgnoreQuestionGroupBy](ctx, tiqgb.build, tiqgb, tiqgb.build.inters, v)
}

func (tiqgb *TestIgnoreQuestionGroupBy) sqlScan(ctx context.Context, root *TestIgnoreQuestionQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tiqgb.fns))
	for _, fn := range tiqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tiqgb.flds)+len(tiqgb.fns))
		for _, f := range *tiqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tiqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tiqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TestIgnoreQuestionSelect is the builder for selecting fields of TestIgnoreQuestion entities.
type TestIgnoreQuestionSelect struct {
	*TestIgnoreQuestionQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tiqs *TestIgnoreQuestionSelect) Aggregate(fns ...AggregateFunc) *TestIgnoreQuestionSelect {
	tiqs.fns = append(tiqs.fns, fns...)
	return tiqs
}

// Scan applies the selector query and scans the result into the given value.
func (tiqs *TestIgnoreQuestionSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tiqs.ctx, ent.OpQuerySelect)
	if err := tiqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestIgnoreQuestionQuery, *TestIgnoreQuestionSelect](ctx, tiqs.TestIgnoreQuestionQuery, tiqs, tiqs.inters, v)
}

func (tiqs *TestIgnoreQuestionSelect) sqlScan(ctx context.Context, root *TestIgnoreQuestionQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tiqs.fns))
	for _, fn := range tiqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tiqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tiqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
