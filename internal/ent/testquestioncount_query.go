// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"
	"template/internal/ent/predicate"
	"template/internal/ent/test"
	"template/internal/ent/testquestioncount"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/google/uuid"
)

// TestQuestionCountQuery is the builder for querying TestQuestionCount entities.
type TestQuestionCountQuery struct {
	config
	ctx        *QueryContext
	order      []testquestioncount.OrderOption
	inters     []Interceptor
	predicates []predicate.TestQuestionCount
	withTest   *TestQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the TestQuestionCountQuery builder.
func (tqcq *TestQuestionCountQuery) Where(ps ...predicate.TestQuestionCount) *TestQuestionCountQuery {
	tqcq.predicates = append(tqcq.predicates, ps...)
	return tqcq
}

// Limit the number of records to be returned by this query.
func (tqcq *TestQuestionCountQuery) Limit(limit int) *TestQuestionCountQuery {
	tqcq.ctx.Limit = &limit
	return tqcq
}

// Offset to start from.
func (tqcq *TestQuestionCountQuery) Offset(offset int) *TestQuestionCountQuery {
	tqcq.ctx.Offset = &offset
	return tqcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (tqcq *TestQuestionCountQuery) Unique(unique bool) *TestQuestionCountQuery {
	tqcq.ctx.Unique = &unique
	return tqcq
}

// Order specifies how the records should be ordered.
func (tqcq *TestQuestionCountQuery) Order(o ...testquestioncount.OrderOption) *TestQuestionCountQuery {
	tqcq.order = append(tqcq.order, o...)
	return tqcq
}

// QueryTest chains the current query on the "test" edge.
func (tqcq *TestQuestionCountQuery) QueryTest() *TestQuery {
	query := (&TestClient{config: tqcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := tqcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := tqcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(testquestioncount.Table, testquestioncount.FieldID, selector),
			sqlgraph.To(test.Table, test.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, false, testquestioncount.TestTable, testquestioncount.TestColumn),
		)
		fromU = sqlgraph.SetNeighbors(tqcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first TestQuestionCount entity from the query.
// Returns a *NotFoundError when no TestQuestionCount was found.
func (tqcq *TestQuestionCountQuery) First(ctx context.Context) (*TestQuestionCount, error) {
	nodes, err := tqcq.Limit(1).All(setContextOp(ctx, tqcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{testquestioncount.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) FirstX(ctx context.Context) *TestQuestionCount {
	node, err := tqcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first TestQuestionCount ID from the query.
// Returns a *NotFoundError when no TestQuestionCount ID was found.
func (tqcq *TestQuestionCountQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tqcq.Limit(1).IDs(setContextOp(ctx, tqcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{testquestioncount.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := tqcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single TestQuestionCount entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one TestQuestionCount entity is found.
// Returns a *NotFoundError when no TestQuestionCount entities are found.
func (tqcq *TestQuestionCountQuery) Only(ctx context.Context) (*TestQuestionCount, error) {
	nodes, err := tqcq.Limit(2).All(setContextOp(ctx, tqcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{testquestioncount.Label}
	default:
		return nil, &NotSingularError{testquestioncount.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) OnlyX(ctx context.Context) *TestQuestionCount {
	node, err := tqcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only TestQuestionCount ID in the query.
// Returns a *NotSingularError when more than one TestQuestionCount ID is found.
// Returns a *NotFoundError when no entities are found.
func (tqcq *TestQuestionCountQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = tqcq.Limit(2).IDs(setContextOp(ctx, tqcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{testquestioncount.Label}
	default:
		err = &NotSingularError{testquestioncount.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := tqcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of TestQuestionCounts.
func (tqcq *TestQuestionCountQuery) All(ctx context.Context) ([]*TestQuestionCount, error) {
	ctx = setContextOp(ctx, tqcq.ctx, ent.OpQueryAll)
	if err := tqcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*TestQuestionCount, *TestQuestionCountQuery]()
	return withInterceptors[[]*TestQuestionCount](ctx, tqcq, qr, tqcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) AllX(ctx context.Context) []*TestQuestionCount {
	nodes, err := tqcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of TestQuestionCount IDs.
func (tqcq *TestQuestionCountQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if tqcq.ctx.Unique == nil && tqcq.path != nil {
		tqcq.Unique(true)
	}
	ctx = setContextOp(ctx, tqcq.ctx, ent.OpQueryIDs)
	if err = tqcq.Select(testquestioncount.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := tqcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (tqcq *TestQuestionCountQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, tqcq.ctx, ent.OpQueryCount)
	if err := tqcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, tqcq, querierCount[*TestQuestionCountQuery](), tqcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) CountX(ctx context.Context) int {
	count, err := tqcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (tqcq *TestQuestionCountQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, tqcq.ctx, ent.OpQueryExist)
	switch _, err := tqcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (tqcq *TestQuestionCountQuery) ExistX(ctx context.Context) bool {
	exist, err := tqcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the TestQuestionCountQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (tqcq *TestQuestionCountQuery) Clone() *TestQuestionCountQuery {
	if tqcq == nil {
		return nil
	}
	return &TestQuestionCountQuery{
		config:     tqcq.config,
		ctx:        tqcq.ctx.Clone(),
		order:      append([]testquestioncount.OrderOption{}, tqcq.order...),
		inters:     append([]Interceptor{}, tqcq.inters...),
		predicates: append([]predicate.TestQuestionCount{}, tqcq.predicates...),
		withTest:   tqcq.withTest.Clone(),
		// clone intermediate query.
		sql:  tqcq.sql.Clone(),
		path: tqcq.path,
	}
}

// WithTest tells the query-builder to eager-load the nodes that are connected to
// the "test" edge. The optional arguments are used to configure the query builder of the edge.
func (tqcq *TestQuestionCountQuery) WithTest(opts ...func(*TestQuery)) *TestQuestionCountQuery {
	query := (&TestClient{config: tqcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	tqcq.withTest = query
	return tqcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.TestQuestionCount.Query().
//		GroupBy(testquestioncount.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (tqcq *TestQuestionCountQuery) GroupBy(field string, fields ...string) *TestQuestionCountGroupBy {
	tqcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &TestQuestionCountGroupBy{build: tqcq}
	grbuild.flds = &tqcq.ctx.Fields
	grbuild.label = testquestioncount.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.TestQuestionCount.Query().
//		Select(testquestioncount.FieldCreatedAt).
//		Scan(ctx, &v)
func (tqcq *TestQuestionCountQuery) Select(fields ...string) *TestQuestionCountSelect {
	tqcq.ctx.Fields = append(tqcq.ctx.Fields, fields...)
	sbuild := &TestQuestionCountSelect{TestQuestionCountQuery: tqcq}
	sbuild.label = testquestioncount.Label
	sbuild.flds, sbuild.scan = &tqcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a TestQuestionCountSelect configured with the given aggregations.
func (tqcq *TestQuestionCountQuery) Aggregate(fns ...AggregateFunc) *TestQuestionCountSelect {
	return tqcq.Select().Aggregate(fns...)
}

func (tqcq *TestQuestionCountQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range tqcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, tqcq); err != nil {
				return err
			}
		}
	}
	for _, f := range tqcq.ctx.Fields {
		if !testquestioncount.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if tqcq.path != nil {
		prev, err := tqcq.path(ctx)
		if err != nil {
			return err
		}
		tqcq.sql = prev
	}
	return nil
}

func (tqcq *TestQuestionCountQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*TestQuestionCount, error) {
	var (
		nodes       = []*TestQuestionCount{}
		_spec       = tqcq.querySpec()
		loadedTypes = [1]bool{
			tqcq.withTest != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*TestQuestionCount).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &TestQuestionCount{config: tqcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, tqcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := tqcq.withTest; query != nil {
		if err := tqcq.loadTest(ctx, query, nodes, nil,
			func(n *TestQuestionCount, e *Test) { n.Edges.Test = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (tqcq *TestQuestionCountQuery) loadTest(ctx context.Context, query *TestQuery, nodes []*TestQuestionCount, init func(*TestQuestionCount), assign func(*TestQuestionCount, *Test)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*TestQuestionCount)
	for i := range nodes {
		fk := nodes[i].TestID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(test.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "test_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (tqcq *TestQuestionCountQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := tqcq.querySpec()
	_spec.Node.Columns = tqcq.ctx.Fields
	if len(tqcq.ctx.Fields) > 0 {
		_spec.Unique = tqcq.ctx.Unique != nil && *tqcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, tqcq.driver, _spec)
}

func (tqcq *TestQuestionCountQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(testquestioncount.Table, testquestioncount.Columns, sqlgraph.NewFieldSpec(testquestioncount.FieldID, field.TypeUUID))
	_spec.From = tqcq.sql
	if unique := tqcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if tqcq.path != nil {
		_spec.Unique = true
	}
	if fields := tqcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, testquestioncount.FieldID)
		for i := range fields {
			if fields[i] != testquestioncount.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if tqcq.withTest != nil {
			_spec.Node.AddColumnOnce(testquestioncount.FieldTestID)
		}
	}
	if ps := tqcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := tqcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := tqcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := tqcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (tqcq *TestQuestionCountQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(tqcq.driver.Dialect())
	t1 := builder.Table(testquestioncount.Table)
	columns := tqcq.ctx.Fields
	if len(columns) == 0 {
		columns = testquestioncount.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if tqcq.sql != nil {
		selector = tqcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if tqcq.ctx.Unique != nil && *tqcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range tqcq.predicates {
		p(selector)
	}
	for _, p := range tqcq.order {
		p(selector)
	}
	if offset := tqcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := tqcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// TestQuestionCountGroupBy is the group-by builder for TestQuestionCount entities.
type TestQuestionCountGroupBy struct {
	selector
	build *TestQuestionCountQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (tqcgb *TestQuestionCountGroupBy) Aggregate(fns ...AggregateFunc) *TestQuestionCountGroupBy {
	tqcgb.fns = append(tqcgb.fns, fns...)
	return tqcgb
}

// Scan applies the selector query and scans the result into the given value.
func (tqcgb *TestQuestionCountGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tqcgb.build.ctx, ent.OpQueryGroupBy)
	if err := tqcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestQuestionCountQuery, *TestQuestionCountGroupBy](ctx, tqcgb.build, tqcgb, tqcgb.build.inters, v)
}

func (tqcgb *TestQuestionCountGroupBy) sqlScan(ctx context.Context, root *TestQuestionCountQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(tqcgb.fns))
	for _, fn := range tqcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*tqcgb.flds)+len(tqcgb.fns))
		for _, f := range *tqcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*tqcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tqcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// TestQuestionCountSelect is the builder for selecting fields of TestQuestionCount entities.
type TestQuestionCountSelect struct {
	*TestQuestionCountQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (tqcs *TestQuestionCountSelect) Aggregate(fns ...AggregateFunc) *TestQuestionCountSelect {
	tqcs.fns = append(tqcs.fns, fns...)
	return tqcs
}

// Scan applies the selector query and scans the result into the given value.
func (tqcs *TestQuestionCountSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, tqcs.ctx, ent.OpQuerySelect)
	if err := tqcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*TestQuestionCountQuery, *TestQuestionCountSelect](ctx, tqcs.TestQuestionCountQuery, tqcs, tqcs.inters, v)
}

func (tqcs *TestQuestionCountSelect) sqlScan(ctx context.Context, root *TestQuestionCountQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(tqcs.fns))
	for _, fn := range tqcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*tqcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := tqcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
