#import "../user/user.fragment.gql"

fragment TestSessionFragment on TestSession {
  id
  createdAt
  updatedAt
  startedAt
  completedAt
  expiredAt
  status
  maxPoints
  pointsEarned
  testId
  userId
  user {
    ...User
  }
} 